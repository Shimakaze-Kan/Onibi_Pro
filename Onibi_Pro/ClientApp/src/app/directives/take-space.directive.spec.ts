import { TakeSpaceDirective } from './take-space.directive';

describe('TakeSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new TakeSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
